# 待办

- [x] 定时同步（2026-08-31）：chrome.alarms 周期推送，设置页可配置间隔分钟数与开关，同步文件沿用 SYNC_FILE 配置

# 已完成

- [x] 多书签文件支持（2026-08-23）：远程 `bookmarks-[name].json` 多文件、推送/拉取文件选择弹窗（含新建）、设置页默认文件下拉（未推送标记）、旧版 `bookmarks.json` 兼容