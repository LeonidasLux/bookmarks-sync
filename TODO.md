定时同步

同步本地浏览器书签到远程仓库的时候，可以指定仓库里的浏览器书签文件，后续远程仓库的文件是可以新增的，以便不同浏览器可以存储不同的书签。
点击同步的时候会从远程仓库拉取所有的浏览器书签文件（格式都是：bookmarks-[name].json），也支持在同步的时候新建一个文件。新建到远程仓库的文件名格式统一为：bookmarks-[name].json，name就是我提供的名字，必须要是英文。

拉取也可以从指定的文件拉取，点击拉取的时候列出文件，选择指定的书签文件进行拉取。

同步文件和从文件拉取这两个都支持在设置里配置默认值，以便操作。

交互方面你来设计。如果你有不确定的问题尽管向我提问，确认好了再开始